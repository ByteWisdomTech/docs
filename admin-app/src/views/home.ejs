<%- include('partials/layout', { title: 'Docusaurus Admin', body: `
  <div class="grid cols-2">
    <div class="card">
      <h2 style="margin-top:0">Welcome${user ? `, ${user.displayName || user.username}` : ''}</h2>
      <p class="muted">Manage your Docusaurus sites, edit docs in a clean UI, and safely open PRs to GitHub.</p>
      ${user ? `<p><a class="btn" href="/repos">Go to Repositories</a></p>` : `<p><a class="btn" href="/auth/github?returnTo=/repos">Login with GitHub</a></p>`}
    </div>
    <div class="card">
      <h3 style="margin-top:0">Tips</h3>
      <ul class="list">
        <li>Edits are saved as Pull Requests for safety.</li>
        <li>Use base branch that matches your repo (main or master).</li>
        <li>Start by importing a repository that has Docusaurus.</li>
      </ul>
    </div>
  </div>
` }) %>
