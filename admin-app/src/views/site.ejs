<%- include('partials/layout', { title: `Edit ${owner}/${repo}`, body: `
  <div class="card">
    <h2 style="margin-top:0">Editing ${owner}/${repo}</h2>
    <form action="/site/${owner}/${repo}/edit" method="GET">
      <label class="muted">Path to edit</label>
      <input type="text" name="path" placeholder="docs/intro.md" value="docs/intro.md"/>
      <div style="margin-top:12px"><button class="btn">Load</button></div>
    </form>
  </div>
` }) %>
